trigger:
  branches:
    include:
      - main4

pr:
  branches:
    include:
      - main4

variables:
  terraformVersion: '1.9.9'
  tfWorkingDirectory: 'Terraform'
  planFile: '$(Pipeline.Workspace)/terraform.plan'

stages:
- stage: TerraformPlan
  displayName: 'Terraform Plan'
  jobs:
  - job: Plan
    displayName: 'Run Terraform init/validate/plan'
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - checkout: self

    - task: AzureCLI@2
      displayName: 'Azure login and Terraform plan'
      inputs:
        azureSubscription: 'Terraform-SP2'
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          sudo apt-get update -y
          sudo apt-get install -y unzip curl
          curl -fsSL https://releases.hashicorp.com/terraform/$(terraformVersion)/terraform_$(terraformVersion)_linux_amd64.zip -o terraform.zip
          unzip -o terraform.zip
          sudo mv terraform /usr/local/bin/
          terraform version

          cd $(tfWorkingDirectory)
          terraform init -reconfigure
          terraform validate
          terraform plan -out="$(planFile)"

- stage: TerraformApply
  displayName: 'Terraform Apply'
  dependsOn: TerraformPlan
  condition: succeeded()
  jobs:
  - job: Apply
    displayName: 'Apply Terraform'
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - checkout: self

    - task: AzureCLI@2
      displayName: 'Azure login and Terraform apply'
      inputs:
        azureSubscription: 'Terraform-SP2'
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          sudo apt-get update -y
          sudo apt-get install -y unzip curl
          curl -fsSL https://releases.hashicorp.com/terraform/$(terraformVersion)/terraform_$(terraformVersion)_linux_amd64.zip -o terraform.zip
          unzip -o terraform.zip
          sudo mv terraform /usr/local/bin/
          terraform version

          cd $(tfWorkingDirectory)
          terraform init -reconfigure
          terraform apply -auto-approve "$(planFile)"

- stage: TerraformDestroy
  displayName: 'Terraform Destroy'
  dependsOn: TerraformApply
  condition: succeeded()
  jobs:
  - job: Destroy
    displayName: 'Destroy Terraform resources'
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - checkout: self

    - task: AzureCLI@2
      displayName: 'Azure login and Terraform destroy'
      inputs:
        azureSubscription: 'Terraform-SP2'
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          sudo apt-get update -y
          sudo apt-get install -y unzip curl
          curl -fsSL https://releases.hashicorp.com/terraform/$(terraformVersion)/terraform_$(terraformVersion)_linux_amd64.zip -o terraform.zip
          unzip -o terraform.zip
          sudo mv terraform /usr/local/bin/
          terraform version

          cd $(tfWorkingDirectory)
          terraform init -reconfigure
          terraform destroy -auto-approve
